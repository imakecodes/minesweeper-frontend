import React from 'react';
import { Header } from 'components';
import './styles.scss';

const Canvas = () => {
  return (
    <div className="canvas">
      <Header />
    </div>
  );
};

export default Canvas;
